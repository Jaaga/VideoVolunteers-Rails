= simple_form_for @state do |f|
  = f.error_notification
  = f.input :name
  = f.input :state_abb, label: state_name_modifier('state_abb')
  = f.input :roi, label: 'Is this an ROI?'
  = f.submit 'Save', class: 'btn btn-primary btn-block'

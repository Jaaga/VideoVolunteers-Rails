%h1.text-center Search Results
%h4.text-center #{params[:search].present? ? "Showing #{@trackers.count} of #{@trackers.total_entries} Search results for '#{params[:search]}'	" : "You gotta search something to get something"}

= will_paginate @trackers if @trackers.present?
%br
-@trackers.each do |tracker|
	.row
		.col-md-2= link_to tracker.uid, tracker
		.col-md-4= tracker.description
		.col-md-4= link_to tracker.cc_name, tracker.cc
		.col-md-2= tracker.production_status
	%hr

= will_paginate @trackers if @trackers.present?